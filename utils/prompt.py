from string import Template

wiki_table_prompt = """
# Role
你是一个专业的历史学家和自然语言处理专家。你的任务是将结构化的表格数据（JSON格式）改写成一段流畅、连贯、且信息密度高的自然语言文本，用于构建高质量的大模型预训练语料库。

# Guidelines
1. **语义融合（关键）**：不要机械地翻译表头（Key）。你需要理解 Key 的含义，将其转化为句子中的连词、介词或上下文。
    - 错误：年份是1560，地点是桶狭间，事件是合战。
    - 正确：1560年，在桶狭间爆发了合战。
2. **翻译与重写（核心）**：
    - 将所有日语内容准确翻译成流畅的中文书面语。
    - **保留专有名词**：人名、地名、官位、历史事件名（如“本能寺の変” -> “本能寺之变”）请使用标准的中文历史通用译名。
    - 不要逐字翻译表头（Key）。理解表头含义，将其转化为句子中的逻辑关系。
3. **剔除冗余表头**：
    - 如果 Key 是“内容”、“详情”、“描述”、“事件”、“说明” (Content, Description, Details) 等通用词，**直接忽略该 Key，只保留 Value**。
    - 不要让这些词出现在最终文本中。
4. **处理时间与地点**：
    - 将时间（Date/Year）放在句首作为背景状语。
    - 将地点（Location/Place）通过“位于”、“在”等介词自然融入。
5. **处理实体关系**：
    - 如果表格包含人物关系（如 Key: "父亲", Value: "织田信秀"），请用自然的句式表达（如“其父为织田信秀”），而不是“父亲是织田信秀”。
6. **空值与碎片处理**：
    - 如果 Value 非常简短（如“无”、“未知”），直接忽略该字段。
    - 如果整行数据只有一列有效信息，直接输出该信息。
7. **文风要求**：
    - 客观、陈述语气。
    - 使用书面语（如维基百科风格）。
    - **严禁臆造**：只基于提供的数据重写，不要编造未出现的事实。

## Example 1 (标准事件：翻译+重组)
Input:
{{
    "headers": ["年", "場所", "出来事"],
    "rows": [
        ["永禄3年", "桶狭間", "織田信長が今川義元を討ち取る。"]
    ]
}}
Output:
{{"lines": ["永禄3年（1560年），织田信长在桶狭间击败并斩杀了今川义元。"]}}

## Example 2 (冗余表头+多行数据)
Input:
{{
    "headers": ["武将", "状況", "詳細"],
    "rows": [
        ["明智光秀", "出陣", "丹波亀山城を出発し、京へ向かう。"],
        ["羽柴秀吉", "対陣", "備中高松城を水攻め中。"]
    ]
}}
Output:
{{
    "lines": [
        "明智光秀处于出阵状态，他离开丹波龟山城，向京都进发。",
        "羽柴秀吉正在与敌军对峙，正对备中高松城实施水攻。"
    ]
}}

## Example 3 (复杂属性/状态描述)
Input:
{{
    "headers": ["名前", "官位", "直前の行動"],
    "rows": [
        ["徳川家康", "三河守", "堺を見物した後、京に戻る予定であった。"]
    ]
}}
Output:
{{
    "lines": ["德川家康，官位三河守，在游览了堺之后，原计划返回京都。"]
}}

# Output要求, 根据下面的 JSON SCHEMA 输出
{json_schema}

# Task
请读取输入的 JSON 数据，将**每一行**数据翻译并改写为一段自然的中文陈述性文本。
请输出一个 JSON 字符串列表 (List of Strings)。

Input Data:
{input_data}
"""

wiki_list_prompt = """
# Role
你是一位精通日本战国历史的NLP数据专家。你的任务是将维基百科（日文）的HTML片段清洗、翻译并重写为高质量的中文自然语言句子，用于训练大语言模型。

# Background
- 数据来源：日本维基百科（日本战国历史相关条目）。
- 目标：构建高质量的中文预训练语料库。
- 原始数据格式：HTML片段，包含多级嵌套的无序列表 `<ul>`、定义列表 `<dl>`，以及结构复杂的表格 `<table>`。

# Goals
1. **结构深度解析**：
   - **处理多级表格**：识别 HTML 结构中的父子层级。将【段落标题】作为根节点，【表头/第一列】作为父节点，【单元格内容】作为子节点。
   - **处理嵌套列表**：识别 `<ul>` 内部嵌套 `<ul>` 的结构。外层 `<li>` 通常是分类、属性或父级实体，内层 `<li>` 是具体成员或子级实体。
2. **翻译与语义化重写 (核心任务)**：
   - 将日文翻译成流畅的中文。
   - **语义补全**：禁止输出孤立的词组。必须根据【上下文 + 父级节点路径】补全主语和谓语。
   - **逻辑推理**：
     - **父子/归属关系**：如果是嵌套列表，外层是“一门众”，内层是“武田信繁”，应重写为“武田信繁是（Context）的一门众成员”。
     - **键值关系**：如果一行中第一列是“正室”，第二列是“浓姬”，重写为“（Context）的正室是浓姬”。
3. **格式输出**：仅输出一个标准的 JSON 格式。

# Logic Rules
- **Rule 1 (Table Hierarchy)**: `Section Title` + `Column Header` + `Row Header` -> `Cell Content`.
- **Rule 2 (Table Key-Value)**: `Row Header (Column 1)` -> `Cell Content (Column 2)`.
- **Rule 3 (Nested Lists)**: `Context` + `Parent <li> Text` -> `Child <li> Text`. (子项必须继承父项的语义).

# Few-Shot Examples

## Example 1 (Simple List)
**User Input:**
Context: "武田信玄的战役"
HTML: <ul><li>川中島的戦い</li><li>三方ヶ原の戦い</li></ul>

**Output:**
[
  "武田信玄曾参与过著名的川中岛之战。",
  "武田信玄在三方原之战中取得了胜利。"
]

## Example 2 (Complex Table - Parent/Child)
**User Input:**
Context: "小牧・长久手之战 - 秀吉军编制"
HTML: 
<table>
  <tr>
    <th>第一队</th>
    <td>池田恒興</td>
    <td>兵力 6,000</td>
  </tr>
  <tr>
    <th>第二队</th>
    <td>森長可</td>
    <td>兵力 3,000</td>
  </tr>
</table>

**Output:**
[
  "在小牧・长久手之战中，秀吉军的第一队由池田恒兴率领，兵力为6,000人。",
  "秀吉军的第二队指挥官是森长可，此时他拥有兵力3,000人。"
]

## Example 3 (Nested Lists - 嵌套列表)
**User Input:**
Context: "德川家康的家族"
HTML:
<ul>
  <li>正室
    <ul>
      <li>築山殿（瀬名）</li>
      <li>朝日姫</li>
    </ul>
  </li>
  <li>側室
    <ul>
      <li>西郷局（お愛の方）</li>
      <li>阿茶局</li>
    </ul>
  </li>
  <li>男子
    <ul>
      <li>長男：松平信康</li>
      <li>次男：結城秀康</li>
    </ul>
  </li>
</ul>

**Output:**
[
  "德川家康的正室包括筑山殿（又名濑名）。",
  "德川家康的另一位正室是朝日姬。",
  "德川家康的侧室包括西乡局（又称爱之方）。",
  "阿茶局也是德川家康备受信任的侧室。",
  "德川家康的长男是松平信康。",
  "德川家康的次男是结城秀康。"
]

# Constraints
- 必须输出纯 JSON 数组格式。
- **降噪处理**：如果父级 `<li>` 仅包含纯文本（作为标题）而没有具体内容，不要为父级单独生成句子，而是将其语义融入子级的句子中。
- 所有的代词（如“他”、“其”）尽量替换为具体的人名或主语，以减少预训练时的指代歧义。

# JSON SCHEMA
{json_schema}

# Context
[维基页面标题]
{page_title}

[条目标题]
{section_title}

# Input Data
{snippet}
"""
a = """
# Role
你是一位精通日本战国历史的数据专家。你的任务是将维基百科（日文）的HTML片段清洗、翻译并重写为**严谨、客观、高信息密度的中文书面语段落**。

# Context
- 数据来源：日本维基百科。
- 目标：构建高密度的中文预训练语料，模拟人类自然写作风格。

# Goals
1. **结构解析**：解析 HTML 层级。
2. **翻译与重写 (核心)**：
   - **聚合处理 (Aggregation)**：对于长列表（超过 3 项的简单枚举），**禁止**拆分为多条重复句式。必须将其合并为一个或两个包含多个并列项的长句。
   - **风格要求**：正式书面语，去口语化。
   - **语义补全**：确保句子主语明确。

# Logic Rules
- **Rule 1 (Hierarchy)**: `Context` + `Header` -> `Content`.
- **Rule 2 (Conditionals)**: 括号内容转为状语。
- **Rule 3 (Aggregation)**: 
    - *Bad Case*: [A是B, C是B, D是B]
    - *Good Case*: [B的成员包括A、C与D。]
    - 如果列表过长（>5项），可分号分隔或拆分为两句（如“...。此外，...也是成员。”）。

# Few-Shot Examples

## Example 1 (Simple Enumeration - 聚合模式)
**User Input:**
Context: "丰臣秀吉设立的十人众"
HTML: 
<ul>
  <li>富田一白</li><li>寺西正勝</li><li>毛利吉成</li><li>堀田一継</li><li>佐々行政</li>
</ul>

**Output:**
[
  "丰臣秀吉设立的十人众成员包括富田一白、寺西正胜、毛利吉成、堀田一继及佐々行政。"
]

## Example 2 (Complex/Detailed List - 保持独立)
*(注：如果每一项都有详细描述，则不适合聚合，保持独立以保留细节)*
**User Input:**
Context: "织田信长的政策"
HTML: 
<ul>
  <li>乐市乐座：废除商业垄断。</li>
  <li>兵农分离：职业军人化。</li>
</ul>

**Output:**
[
  "织田信长推行乐市乐座政策，旨在废除商业垄断。",
  "织田信长实施兵农分离，推动了军队的职业化。"
]

## Example 3 (Inline Split + Aggregation - 混合处理)
**User Input:**
Context: "丰臣五大老"
HTML: <dd>徳川家康、前田利家、上杉景勝、宇喜多秀家、毛利輝元</dd>

**Output:**
[
  "丰臣政权五大老由德川家康、前田利家、上杉景胜、宇喜多秀家及毛利辉元共同担任。"
]

# Constraints
- 仅输出 JSON 数组。
- **优先合并**：对于纯名词列表，务必合并成自然段落风格的长句。
- **标点规范**：并列项使用顿号（、），最后一个连接词使用“及”或“与”。

# JSON SCHEMA
{json_schema}

# Context
[维基页面标题]
{page_title}

[条目标题]
{section_title}

# Input Data
{snippet}
"""
