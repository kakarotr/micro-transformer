from string import Template

wiki_table_prompt = """
# Role
你是一个专业的历史学家和自然语言处理专家。你的任务是将结构化的表格数据（JSON格式）改写成一段流畅、连贯、且信息密度高的自然语言文本，用于构建高质量的大模型预训练语料库。

# Guidelines
1. **语义融合（关键）**：不要机械地翻译表头（Key）。你需要理解 Key 的含义，将其转化为句子中的连词、介词或上下文。
    - 错误：年份是1560，地点是桶狭间，事件是合战。
    - 正确：1560年，在桶狭间爆发了合战。
2. **翻译与重写（核心）**：
    - 将所有日语内容准确翻译成流畅的中文书面语。
    - **保留专有名词**：人名、地名、官位、历史事件名（如“本能寺の変” -> “本能寺之变”）请使用标准的中文历史通用译名。
    - 不要逐字翻译表头（Key）。理解表头含义，将其转化为句子中的逻辑关系。
3. **剔除冗余表头**：
    - 如果 Key 是“内容”、“详情”、“描述”、“事件”、“说明” (Content, Description, Details) 等通用词，**直接忽略该 Key，只保留 Value**。
    - 不要让这些词出现在最终文本中。
4. **处理时间与地点**：
    - 将时间（Date/Year）放在句首作为背景状语。
    - 将地点（Location/Place）通过“位于”、“在”等介词自然融入。
5. **处理实体关系**：
    - 如果表格包含人物关系（如 Key: "父亲", Value: "织田信秀"），请用自然的句式表达（如“其父为织田信秀”），而不是“父亲是织田信秀”。
6. **空值与碎片处理**：
    - 如果 Value 非常简短（如“无”、“未知”），直接忽略该字段。
    - 如果整行数据只有一列有效信息，直接输出该信息。
7. **文风要求**：
    - 客观、陈述语气。
    - 使用书面语（如维基百科风格）。
    - **严禁臆造**：只基于提供的数据重写，不要编造未出现的事实。

## Example 1 (标准事件：翻译+重组)
Input:
{{
    "headers": ["年", "場所", "出来事"],
    "rows": [
        ["永禄3年", "桶狭間", "織田信長が今川義元を討ち取る。"]
    ]
}}
Output:
{{"lines": ["永禄3年（1560年），织田信长在桶狭间击败并斩杀了今川义元。"]}}

## Example 2 (冗余表头+多行数据)
Input:
{{
    "headers": ["武将", "状況", "詳細"],
    "rows": [
        ["明智光秀", "出陣", "丹波亀山城を出発し、京へ向かう。"],
        ["羽柴秀吉", "対陣", "備中高松城を水攻め中。"]
    ]
}}
Output:
{{
    "lines": [
        "明智光秀处于出阵状态，他离开丹波龟山城，向京都进发。",
        "羽柴秀吉正在与敌军对峙，正对备中高松城实施水攻。"
    ]
}}

## Example 3 (复杂属性/状态描述)
Input:
{{
    "headers": ["名前", "官位", "直前の行動"],
    "rows": [
        ["徳川家康", "三河守", "堺を見物した後、京に戻る予定であった。"]
    ]
}}
Output:
{{
    "lines": ["德川家康，官位三河守，在游览了堺之后，原计划返回京都。"]
}}

# Output要求, 根据下面的 JSON SCHEMA 输出
{json_schema}

# Task
请读取输入的 JSON 数据，将**每一行**数据翻译并改写为一段自然的中文陈述性文本。
请输出一个 JSON 字符串列表 (List of Strings)。

Input Data:
{input_data}
"""
