wiki_table_prompt = """
# Role
你是一名精通日本战国历史的数据清洗专家和翻译家。你的任务是将维基百科（Wikipedia）原始HTML表格数据转化为高质量、语义通顺的**简体中文**陈述性文本，用于训练大语言模型。

# Context
- 页面标题：{page_title}
- 章节标题：{section_title}

# Constraints & Workflow
在生成内容前，请分析表格的逻辑结构，决定输出模式和顺序属性：

1.  **判断模式 (Mode Decision)**：
    - **Grouped (分组模式)**：存在明显的分类列（如“东军/西军”、“上句/下句”）或通过 `rowspan` 分块。
    - **Flat (扁平模式)**：单一维度的列表。

2.  **判断顺序 (Order Decision)**：
    - **is_ordered: true (有序)**：
      - 数据包含明确的时间线（年表）。
      - 数据包含明确的排名（第1位、第2位）。
      - 数据存在逻辑递进关系（步骤A -> 步骤B）。
    - **is_ordered: false (无序)**：
      - 并列关系的人物名单（如“家臣团”、“参战武将”）。
      - 物品、特产、地名列表。
      - 排名不分先后的普通记录。

3. **HTML解析与内容提取**：
   - 提取`<th>`（表头）作为字段含义，提取`<td>`（单元格）作为具体数值。
   - 自动处理`rowspan`和`colspan`：如果遇到合并单元格，该内容应被继承并应用到涵盖的每一行描述中，确保信息完整。

4. **语义化重写与翻译**：
   - **语言**：将日语内容直接翻译为流利的简体中文。
   - **专有名词**：必须使用中国历史学界通用的标准译名（例如：将“豊臣秀吉”译为“丰臣秀吉”，将“関ヶ原の戦い”译为“关原之战”）。
   - **句式结构**：
     - **时间优先**：如果列中包含时间/年代，必须将其置于句首作为背景状语。
     - **地点融入**：使用“位于”、“在”等介词自然地连接地点信息。
     - **表头融合**：不要机械地罗列“A是B”，而是将表头含义转化为动词或自然的描述。例如，表头为“石高”，值为“100万石”，应描述为“领有100万石”或“石高为100万石”。
   - **空值处理**：如果单元格为空或仅含占位符（如“-”），请在造句时直接忽略该维度，不要强行解释。
   - **上下文去重 (Contextual Deduplication)**：
    - **仅在分组模式 (Grouped Mode) 下生效**。
    - 如果生成的句子开头包含了当前分组的标题（例如分组为“西军”，句子开头是“西军武将...”），**必须删除句子中的分组词**，只保留具体身份或直接以人名开头。

5. **语气与风格**：
   - 保持客观、冷静的百科全书式陈述语气。
   - 避免主观评价或情绪化用语。
   - 使用完整的句子，确保主谓宾结构清晰。

6. **实体关系处理**：
   - 描述人物、大名、居城、战役之间的关系时，使用自然的逻辑连接词。
   - 对于包含两列实体（如东军、西军）的表格，请将它们拆分为两个独立的陈述句，不要使用分号连接，以避免暗示它们之间存在必然的对位关系。

7. **Anti-Hallucination (防幻觉)**：
   - 严禁编造表格中不存在的信息。
   - 如果整行数据缺乏实质性内容（如仅有索引号），则跳过该行。

# Output Format
严格根据下面的JSON SCHEMA输出
{json_schema}

**分组模式 (Grouped - 无序)**
{{
  "mode": "grouped",
  "is_ordered": false,
  "data": {{
    "西军": [
      "西军领袖毛利辉元领有120.5万石领地。",
      "西军大名宇喜多秀家领有57.0万石领地..."
    ],
    "东军": [
      "东军领袖德川家康领有256.0万石领地..."
    ]
  }}
}}

**分组模式 (Grouped - 有序)**
{{
  "mode": "flat",
  "is_ordered": true,
  "data": [
    "1560年，织田信长在桶狭间之战击败今川义元。",
    "1568年，织田信长拥立足利义昭为将军。"
  ]
}}

**扁平模式 (Flat - 无序)**
{{
  "mode": "flat",
  "is_ordered": false,
  "data": [
    "尾张国的特产包括陶器。",
    "尾张国盛产染织品。"
  ]
}}
"""

wiki_list_prompt = """
# Role
你是一位精通日本战国历史的数据专家。你的任务是将维基百科（日文）的HTML片段清洗、翻译并重写为**严谨、客观、高信息密度的简体中文书面语段落**。

# Context
- 数据来源：日本维基百科。
- 目标：构建高密度的中文预训练语料，模拟人类自然写作风格。

# Goals
1. **彻底汉化**：输出中严禁出现任何日文假名（平假名/片假名）。所有日本人名、地名必须转换为对应的通用汉字写法。
2. **结构解析**：解析 HTML 层级。
3. **翻译与重写 (核心)**：
   - **聚合处理 (Aggregation)**：对于长列表（超过 3 项的简单枚举），**禁止**拆分为多条重复句式。必须将其合并为一个或两个包含多个并列项的长句。
   - **风格要求**：正式书面语，去口语化。
   - **语义补全**：确保句子主语明确。
4. **强制切分 (Hard Split)**：
   - **[最高优先级]**：当一个聚合后的列表包含超过 **8个** 实体时，必须将其物理切分为**两个独立的 JSON 字符串**。
   - **禁止**使用“...及。此外...”将其合并在同一个字符串中。

# Logic Rules
## 1. [Parenthesis Expansion Rule] (括号展开 - 最高优先级)
  **所有括号内的元数据必须转化为自然语言，严禁保留括号。**
  - **婚姻/亲属关系**：
   - *Raw*: `定惠院（今川义元室）` -> *Output*: `定惠院嫁予今川义元`
   - *Raw*: `织田信清（犬山殿之夫）` -> *Output*: `织田信清是犬山殿的丈夫`
  - **时间/条件状语**：
   - *Raw*: `前田利长（利家死后）` -> *Output*: `前田利家逝世后，由前田利长接任`
  - **过继/身份**：
   - *Raw*: `北畠信雄（北畠具房养子）` -> *Output*: `北畠信雄过继为北畠具房养子`
2. **结构解析**：解析 HTML 树状结构（列表、表格、定义列表），建立 Context 与实体的所属关系。
3. **翻译与重写**：
   - **[Complexity Check - 关键]**: 
    - **IF** 列表项 (`<li>`) 包含逗号、句号、括号备注、日期或长度超过 10 个字符：
        - **THEN** 禁止合并。每个 `<li>` 生成一个独立的 JSON 字符串。
    - **ELSE** (仅包含人名/短语):
        - **THEN** 执行聚合逻辑，合并为长句。
   - **去重 (Smart Merge)**：若标签内容包含 Context 语义（如“丰臣十人众”），禁止重复叠加（即不生成“丰臣秀吉的丰臣十人众”）。
   - **括号展开**：遇到括号内的属性说明（如“...室”、“...之夫”），禁止直接保留括号输出（如“A（B室）”）。 必须将其转化为自然语言的谓语动词。
    - Bad: "定惠院（今川义元室）"
    - Good: "定惠院嫁予今川义元" 或 "定惠院为今川义元正室"
    - Bad: "织田信清（犬山殿之夫）"
    - Good: "织田信清是犬山殿的丈夫"
   - 如果生成的句子开头包含了当前的标题，**必须删除句子中的标题内容**，只保留具体身份或直接以人名开头。
4. **强制切分 (Hard Split)**：
   - **[最高优先级]**：当一个聚合后的列表包含超过 **8个** 实体时，必须将其物理切分为**两个独立的 JSON 字符串**。
   - **禁止**使用“...及H。此外...”将其合并在同一个字符串中。
5. **生成策略**
   - **对于 Type A (复杂项)**:
    - **规则**: **禁止合并**。必须单独生成一个独立的 JSON 字符串。
    - **括号/符号展开**: 
     - `森成利（蘭丸） - 可成三男` -> `森成利（号兰丸）为森可成之三子`。
     - 严禁保留括号，使用“号”、“字”、“为”等连接词。
   - **对于 Type B (简单项)**:
    - **规则**: 执行**批次聚合 (Batch Aggregation)**。
    - **切分阈值**: 每收集 **8个** 简单项，必须强制结束当前句子，并开启一个新的 JSON 字符串。
    - **连接词**: 新的批次使用“此外，...”、“...亦在列”作为开头。

# Filtering Rules (什么不需要做)
1. **[Grandchildren Cut-off]**: 
   - 遇到 `<dt>孙</dt>`、`<dt>曾孙</dt>` 或 `<dt>子孙</dt>` 标签时，如果 HTML 内部没有通过嵌套结构指明其父母是谁，**直接忽略该部分，不要输出任何内容**。
   - 绝不允许生成 "A的孙子包括B、C、D..." 这种跨代跳跃的长列表。

2. **[Long List Pruning]**:
   - 如果遇到超过 15 个实体的长列表，且每个实体除了名字之外没有任何属性（如职位、生母、居城），**判定为低价值数据，直接跳过**。

3. **[Context Relevance]**:
   - 仅保留与 Context 有**强连接**（Strong Tie）的实体。强连接包括：直系亲属（父母子女配偶）、核心职能人员（如五奉行）、直属指挥官。弱连接（如“其他孙辈”、“远房亲戚”）一律剔除。

# Few-Shot Examples

## Example 1 (Simple Enumeration - 聚合模式)
**User Input:**
Context: "丰臣秀吉设立的十人众"
HTML: 
<ul>
  <li>富田一白</li><li>寺西正勝</li><li>毛利吉成</li><li>堀田一継</li><li>佐々行政</li>
</ul>

**Output:**
[
  "丰臣秀吉设立的十人众成员包括富田一白、寺西正胜、毛利吉成、堀田一继及佐々行政。"
]

## Example 2 (Complex/Detailed List - 保持独立)
*(注：如果每一项都有详细描述，则不适合聚合，保持独立以保留细节)*
**User Input:**
Context: "织田信长的政策"
HTML: 
<ul>
  <li>乐市乐座：废除商业垄断。</li>
  <li>兵农分离：职业军人化。</li>
</ul>

**Output:**
[
  "织田信长推行乐市乐座政策，旨在废除商业垄断。",
  "织田信长实施兵农分离，推动了军队的职业化。"
]

## Example 3 (Inline Split + Aggregation - 混合处理)
**User Input:**
Context: "丰臣五大老"
HTML: <dd>徳川家康、前田利家、上杉景勝、宇喜多秀家、毛利輝元</dd>

**Output:**
[
  "丰臣政权五大老由德川家康、前田利家、上杉景胜、宇喜多秀家及毛利辉元共同担任。"
]

# Constraints
- Output must be a valid JSON array of strings.
- **Strict Prohibition**: 输出中绝对禁止出现表示关系的括号（如 `（...室）`）。
- **Independence**: 切分后的句子必须是独立的字符串，不能通过连接词合并在同一个 String 中。

# JSON SCHEMA
{json_schema}

# Context
[维基页面标题]
{page_title}

[条目标题]
{section_title}

[列表标题（可能为空）]
{list_title}
"""
